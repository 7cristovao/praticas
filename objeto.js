let pessoa = {
    //chave: "valor",
    nome: "Alex",
    time: "Corinthians",
    altura: 1.5, 
    trabalhando: true
    // obs: não usar números como chave de um objeto
}

console.log(pessoa['nome']) // resgata o atributo nome (posição)
console.log(pessoa.time) // resgata pela chave (posição)
console.log(pessoa)